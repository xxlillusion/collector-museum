import PageShell, { ComingSoon } from '../PageShell';

// Owned by the accounts workstream (Stream A).
export default function AccountScreen() {
  return (
    <PageShell title="My Account">
      <ComingSoon note="Profile, roles and the local-collection import wizard land with the accounts workstream." />
    </PageShell>
  );
}
