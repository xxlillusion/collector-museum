import PageShell, { ComingSoon } from '../PageShell';

// Owned by the accounts workstream (Stream A).
export default function SignupScreen() {
  return (
    <PageShell title="Create Account">
      <ComingSoon note="Account creation lands with the accounts workstream — collector, vendor and organizer signup." />
    </PageShell>
  );
}
