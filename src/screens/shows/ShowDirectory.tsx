import PageShell, { ComingSoon } from '../PageShell';

// Owned by the shows workstream (Stream C).
export default function ShowDirectory() {
  return (
    <PageShell title="Card Shows">
      <ComingSoon note="Browse upcoming card shows, view their floor plans and walk them in 3D — landing with the shows workstream." />
    </PageShell>
  );
}
