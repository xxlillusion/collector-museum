import PageShell, { ComingSoon } from '../PageShell';

// Owned by the accounts workstream (Stream A).
export default function LoginScreen() {
  return (
    <PageShell title="Sign In">
      <ComingSoon note="Accounts are on the way — collectors, vendors and show organizers will sign in here." />
    </PageShell>
  );
}
