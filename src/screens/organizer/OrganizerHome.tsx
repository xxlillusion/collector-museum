import PageShell, { ComingSoon } from '../PageShell';

// Owned by the shows workstream (Stream C).
export default function OrganizerHome() {
  return (
    <PageShell title="Organizer">
      <ComingSoon note="Create and publish shows — upload a floor plan, edit booths, assign vendors. Lands with the shows workstream." />
    </PageShell>
  );
}
