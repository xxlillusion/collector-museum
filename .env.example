# Supabase project credentials (Settings -> API in the Supabase dashboard).
# Copy to .env.local (gitignored). When absent, the app runs guest-only:
# IndexedDB persistence, no auth UI — exactly the pre-platform experience.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
